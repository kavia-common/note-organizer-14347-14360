# Tools

If your CI cannot execute `./gradlew`, use this fallback:

- bash note-organizer-14347-14360/tools/gradle-check.sh

It fixes permissions and runs the Gradle check via the bootstrapping shim.
