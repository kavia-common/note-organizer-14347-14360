#!/usr/bin/env sh
# Conventional CI entrypoint that some analyzers call by default.
set -eu
DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
sh "$DIR/set-exec-gradle.sh" || true
exec sh "$DIR/run-gradle-check-bootstrap.sh"
