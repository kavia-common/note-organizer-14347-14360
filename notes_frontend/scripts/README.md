# Scripts

- seed-demo.ts
  Seeds demo folders and notes into AsyncStorage keys used by the app:
  - notes.data.v1
  - folders.data.v1

Usage (Expo / Node):
- From app directory:
  cd note-organizer-14347-14360/notes_frontend
  npx ts-node scripts/seed-demo.ts
  (or compile to JS and run with node)
