# Theme — Ocean Professional

Tokens provided in `colors.ts`:
- Colors: primary (#2563EB), secondary (#F59E0B), error (#EF4444), background (#f9fafb), surface (#ffffff), text (#111827), textMuted, border, shadow
- Radii: xs, sm, md, lg, xl, round
- Spacing: xs, sm, md, lg, xl, xxl
- Elevation: sm, md (shadows for subtle depth)
- Gradients: header, card (subtle tints)

Usage:
- Prefer tokens over hard-coded values for consistency
- Keep gradients subtle to maintain the minimal, modern aesthetic
- Apply rounded corners and small shadows to cards and primary UI elements
