# Navigation Exports

Use this import to access the typed Route union across the app:

```ts
import type { Route } from '../navigation';
```

The barrel (`src/navigation/index.ts`) re-exports `Route` from `types.ts`.
