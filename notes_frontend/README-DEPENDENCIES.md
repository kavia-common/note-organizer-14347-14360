Dependencies

- @react-native-async-storage/async-storage (^2.0.0) is used for local persistence.
- For native Android builds, run Expo prebuild to generate the Android project and Gradle wrapper:
  npm install
  npm run prebuild:android

Run app in Expo Go:
  npm start
