# Scripts

- fetch-gradle-wrapper-jar.sh
  Ensures `gradle/wrapper/gradle-wrapper.jar` exists by downloading the Gradle distribution specified in `gradle/wrapper/gradle-wrapper.properties`. This supports CI environments where the wrapper JAR is not pre-committed.
