# Helper hint for CI tools: The actual Gradle wrapper for this project lives in notes_frontend/android/gradlew.
# Some CI scanners look for gradle wrapper metadata at repository root.
# Use the provided shim scripts or invoke the wrapper at:
#   notes_frontend/android/gradlew
# Or the root shim:
#   ./gradlew  (forwards to notes_frontend/android/gradlew)
# Or the helper script:
#   ./run-android-gradle.sh :app:assembleDebug
wrapper.path=notes_frontend/android/gradlew
