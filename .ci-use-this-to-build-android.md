Use this script to build Android from CI instead of calling ./gradlew directly:
- bash ./build-android.sh

This script:
1) Ensures dependencies are installed
2) Runs Expo prebuild to generate the Android wrapper if missing
3) Invokes the internal Gradle wrapper to assemble the app (assembleDebug)
