#!/usr/bin/env bash
# Lightweight shim variant for analyzers probing 'gradlew.lite'.
# Delegates to the main gradlew shim which handles bootstrapping and proxying.

set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT_DIR/gradlew" "$@"
