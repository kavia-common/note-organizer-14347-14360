plugins {
    // Keep minimal; actual Android app is generated by Expo.
    // This file exists so Gradle wrapper can run tasks like 'help' or 'check' without a full project.
}
task check {
    doLast {
        println("Placeholder check: Android project is managed by Expo Prebuild. No-op.")
    }
}
